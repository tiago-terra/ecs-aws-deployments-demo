apiVersion: v1
kind: Service
metadata:
  name: service-deploy
  labels:
    app: deploy-demo
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    app: deploy-demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-demo
  name: deployment-demo-rolling
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deploy-demo
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: deploy-demo
    spec:
      containers:
        - image: $IMAGE_URI
          name: deploy-demo-container
          imagePullPolicy: $IMAGE_PULL
          resources: {}
          env:
            - name: DEPLOY_TYPE
              value: rolling
          ports:
            - name: web
              containerPort: 80
              protocol: TCP
status: {}
