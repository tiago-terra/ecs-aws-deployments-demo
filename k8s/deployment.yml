apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${TYPE}-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      name: web-container
      app: ${TYPE}-app
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: ${TYPE}-app
        name: web-container
    spec:
      containers:
        - image: $ECR_REPO:$IMAGE_TAG
          name: web-container
          imagePullPolicy: Always
          resources: {}
          env:
            - name: DEPLOY_TYPE
              value: ${TYPE}
            - name: DEPLOY_COLOR
              value: ${TYPE}
            - name: COMMIT_VERSION
              value: ${IMAGE_TAG}
          ports:
            - name: web
              containerPort: 80
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: 80
status: {}
