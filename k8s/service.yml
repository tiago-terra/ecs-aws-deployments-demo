apiVersion: v1
kind: Service
metadata:
  name: ${TYPE}-lb
  labels:
    app: ${TYPE}-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    app: ${TYPE}-app
