apiVersion: v1
kind: Service
metadata:
  name: demo-lb
  labels:
    app: deploy-demo
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    app: $TYPE-app
