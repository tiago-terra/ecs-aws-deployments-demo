apiVersion: v1
kind: Service
metadata:
  name: demo-lb
  labels:
    app: ${TYPE}-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: web
  selector:
    name: web-container
    app: ${TYPE}-app
