version: 0.2

env:
  variables:
    IMAGE_PULL: "ALWAYS"
    KUBE_URL: "https://amazon-eks.s3.us-west-2.amazonaws.com/1.15.10/2020-02-22/bin/linux/amd64/kubectl"
    MANIFEST: "k8s/manifest.yml"
phases:
  install:
    commands:
      - curl -o kubectl $KUBE_URL && chmod +x ./kubectl
      - mkdir -p $HOME/bin && cp ./kubectl $HOME/bin/kubectl && export PATH=$PATH:$HOME/bin
      - echo 'export PATH=$PATH:$HOME/bin' >> ~/.profile
  pre_build:
    commands:
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_URI=$ECR_REPO:$IMAGE_TAG
      - aws eks --region $AWS_DEFAULT_REGION update-kubeconfig --name $EKS_CLUSTER_NAME
      - envsubst '$DEPLOY_TYPE $IMAGE_URI $IMAGE_PULL' < "${MANIFEST}" > "${MANIFEST}_${DEPLOY_TYPE}.yml"
  build:
    commands:
      - echo Push the latest image to cluster
      - kubectl apply -f manifest_$DEPLOY_TYPE.yml
